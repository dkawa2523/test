# パルス印加時のイオン入射異方性と穴形状傾向の評価方法

## まとめ
### 目的と評価対象

下部電極へのパルス印加によってウェハー近傍の電場が時間的に変化すると、ウェハーへ到達するイオンの半径方向速度も変化する。本検討では、この変化が実験で観察される穴の楕円化や真円度低下と関係しているかを評価する。

評価は、次の2段階に分ける。

1. COMSOLの粒子追跡結果から、ウェハー到達時のイオン入射分布の異方性を求める。
2. 穴径と深さを考慮した簡易的な底部到達スポットを計算し、実験穴輪郭の楕円成分と比較する。

この計算から直接得られるのは、実加工穴の真円度そのものではない。得られるのは、イオン入射分布が穴形状を一方向に変形させる傾向である。

実際の穴形状には、側壁遮蔽、表面帯電、イオン反射、ラジカル、パッシベーション、材料ごとのエッチング収率なども影響する。このため、計算指標と実験指標の傾向および方向を比較し、仮説の妥当性を判断する。

### 最終的に使用する指標

| 区分 | 指標 | 評価する値 | 値の読み方 |
|---|---|---|---|
| 計算側の主指標 | $Q_{\mathrm{spot}}(R)$ | 穴深さ位置におけるイオン到達スポットの短軸幅／長軸幅 | 1に近いほど等方的。小さいほど一方向に広がる |
| 実験側の主指標 | $\varepsilon_2(R,\Theta)$ | 再中心化した穴輪郭に含まれる楕円変形の大きさ | 0に近いほど楕円成分が小さい |
| 実験側の方向指標 | $\psi_2(R,\Theta)$ | 楕円変形の主軸方向 | 半径方向か接線方向かを確認する |
| 原因分析 | $\mu_R(R)$ | イオン束全体の平均半径方向傾斜 | 穴軸の傾き、底部中心ずれの要因になる |
| 原因分析 | $\sigma_{\mathrm{swing},R}(R)$ | パルス位相による平均入射方向の変動 | 大きいほどパルスに同期した振れが大きい |
| 原因分析 | $Q_{\mathrm{ion}}(R)$ | ウェハー面における入射角度分布の短軸幅／長軸幅 | 1に近いほど半径・接線方向の幅が等しい |
| 加工量の確認 | $\Gamma_{\mathrm{eff}}(R)$ | エッチングに寄与するイオンの単位面積・単位時間当たりドーズ | 小さい領域では形状指標だけでなく加工不足も疑う |

計算と実験の主比較は、

$$
1-Q_{\mathrm{spot}}(R)
\quad\text{と}\quad
\varepsilon_2(R,\Theta)
$$

の半径方向傾向で行う。

両者を同じ物理量として等号で結ばない。装置条件変更やウェハー半径に対して、同じ増減傾向を示すかを確認する。

### 最終的な判定

パルスによるイオン入射異方性が穴の楕円化へ寄与していると判断するためには、少なくとも次の対応が必要である。

1. $1-Q_{\mathrm{spot}}$ が大きくなる半径または条件で、実験の $\varepsilon_2$ も大きくなる。
2. パルス振幅、Duty比、圧力などを変えたとき、計算と実験の増減方向が一致する。
3. 実験穴の楕円主軸が、局所的な半径方向または接線方向に分布する。
4. 平均入射傾斜 $\mu_R$ は穴中心ずれ、分布異方性 $1-Q_{\mathrm{spot}}$ は再中心化後の楕円変形と対応する。

一方、同一半径で装置方位角ごとの差が大きい場合や、楕円主軸がウェハー全体で同じ装置固定方向を向く場合は、2D軸対称モデル以外の要因を考える。

### 本評価の前提

1. 電場と粒子軌道は、2D軸対称の $R$–$z$ モデルで計算する。
2. 電場の接線方向成分はなく、パルスによる系統的な加速は半径方向と法線方向に生じるものとする。
3. 接線方向速度 $v_\theta$ は、粒子放出時の初期熱速度とする。
4. 接線方向の衝突散乱、方位方向電場、非軸対称な装置構造の影響は含めない。
5. 接線方向熱速度は半径方向運動と独立で、平均0の対称分布とする。
6. 各指標は、過渡状態ではなく、パルス周期に対して周期定常となった衝突粒子から計算する。
7. ウェハー上の各半径位置は、十分な衝突粒子数を確保できる細い環状領域に分けて評価する。
8. 実験穴の楕円成分は、穴中心を再計算してから求める。中心ずれと楕円変形を混同しない。

2D軸対称モデルで予測できるのは、指標の半径 $R$ 依存と、変形方向が局所的な半径方向または接線方向になる傾向までである。同じ半径における装置方位角 $\Theta$ ごとの差は、このモデルでは予測できない。

### 計算に必要なCOMSOL出力

#### 衝突イベントごとの必須出力

ウェハーへ衝突した粒子について、1衝突イベントを1行として保存する。

| 出力項目 | 記号 | 単位 | 用途 |
|---|---:|---:|---|
| 衝突半径位置 | $R_i$ | m | ウェハー半径ごとの集計 |
| 衝突時刻 | $t_i$ | s | パルス衝突位相の算出 |
| 衝突時の半径方向速度 | $v_{R,i}$ | m/s | 半径方向入射傾斜の算出 |
| 衝突時の法線方向速度 | $v_{z,i}$ | m/s | 入射傾斜の基準速度 |
| マクロ粒子重み | $W_i$ | ― | 実粒子数に対応した重み付け |
| イオン種 | $s_i$ | ― | 質量、温度、収率の選択 |

ウェハーへ向かう法線方向到達速度は、座標の符号に依存しないように

$$
u_{n,i}=|v_{z,i}|>0
$$

と定義する。

#### 計算条件として必要な値

以下は粒子ごとの出力ではなく、計算条件または実験条件として記録する。

| 条件 | 記号 | 用途 |
|---|---:|---|
| パルス周期 | $T_{\mathrm p}$ | 衝突時刻からパルス位相を求める |
| パルス位相の基準時刻 | $t_0$ | 波形と衝突位相を対応させる |
| イオン種ごとの初期温度 | $T_{i,s}$ | 接線方向熱速度を求める |
| イオン種ごとの質量 | $m_s$ | 接線方向熱速度を求める |
| 穴入口半径 | $a$ | 底部到達スポットを求める |
| 評価深さ | $H$ | 角度分布を横方向位置分布へ変換する |
| 評価対象のパルス周期範囲 | ― | 過渡期間を除外する |

#### 可能であれば出力する値

| 出力項目 | 用途 |
|---|---|
| 衝突時のイオンエネルギー $E_i$ | エッチング収率による重み付け |
| 粒子放出時刻 | 飛行時間と位相遅れの確認 |
| 初期半径方向速度 | 初期熱速度と電場加速の分離 |
| 衝突回数・衝突種別 | 電荷交換、弾性衝突の影響確認 |
| 衝突直前の $E_R,E_z$ | 軌道変化とシース電場の対応確認 |
| 粒子ID | 軌道履歴との対応、重複計数の確認 |

平均値や標準偏差だけをCOMSOLから出力するのではなく、できる限り衝突イベント単位で保存する。イベントデータがあれば、後から半径幅、位相ビン数、重み付け方法を変更できる。

### 計算の流れ

1. 周期定常後のウェハー衝突イベントを抽出する。
2. 衝突粒子をウェハー半径とパルス位相で分類する。
3. 半径方向入射傾斜 $p_R=v_R/|v_z|$ を求める。
4. パルス位相間の変動と、同一位相内の粒子ばらつきを分離する。
5. 初期イオン温度から接線方向熱分布幅を求める。
6. 半径方向と接線方向の分布幅から $Q_{\mathrm{ion}}$ を求める。
7. 穴入口半径と評価深さを加え、最終指標 $Q_{\mathrm{spot}}$ を求める。
8. 実験穴輪郭から $\varepsilon_2,\psi_2$ を求め、半径方向傾向と方向を比較する。

------


## 1. 座標と物理的な考え方

### 1.1 座標

ウェハー上の位置と、1個の穴内の位置を区別する。

| 記号 | 意味 |
|---|---|
| $R$ | ウェハー中心からの半径位置 |
| $\Theta$ | ウェハー上の装置方位角 |
| $z$ | ウェハー法線方向 |
| $\rho$ | 1個の穴中心から輪郭までの距離 |
| $\varphi$ | 1個の穴内の輪郭方位角 |
| $i$ | ウェハーへ衝突した粒子イベント番号 |
| $b$ | パルス周期を分割した位相ビン番号 |

$\Theta$ はウェハー上の穴配置を表し、$\varphi$ は各穴の輪郭を表す。両者を同じ記号で表さない。

以下の式は、1つの半径方向環状領域について計算するものとする。実際には環状領域ごとに同じ計算を行い、各指標を $R$ の関数として整理する。

### 1.2 半径方向と接線方向の扱い

本モデルでは、横方向運動を次の2方向に分ける。

- 半径方向：COMSOLで計算した衝突時速度 $v_R$ を使用する。
- 接線方向：初期イオン温度から与えた熱速度 $v_{\theta0}$ を使用する。

半径方向速度には、初期熱速度だけでなく、電場、パルス位相、衝突、半径位置による影響が含まれる。接線方向速度には、これらの系統的な加速を含めない。

この差を使って、パルスによって半径方向の角度分布だけが広がるという仮説を評価する。

---

## 2. イオン入射方向の定義

### 2.1 半径方向入射傾斜

粒子 $i$ の半径方向入射傾斜を

$$
p_{R,i}=\frac{v_{R,i}}{u_{n,i}}
$$

と定義する。

- $p_{R,i}>0$：ウェハー外周方向へ傾いて入射
- $p_{R,i}<0$：ウェハー中心方向へ傾いて入射
- $p_{R,i}=0$：半径方向には傾かず、法線方向へ入射

$p_{R,i}$ は無次元であり、半径方向入射角を $\alpha_{R,i}$ とすると

$$
p_{R,i}=\tan\alpha_{R,i}
$$

の関係にある。入射角そのものではなく傾斜を使用することで、内向きと外向きの符号を保持できる。

### 2.2 接線方向初期熱速度

イオン種 $s$ の接線方向初期熱速度は、平均0、分散 $k_{\mathrm B}T_{i,s}/m_s$ の1次元Maxwell分布に対応する正規分布として与える。

$$
v_{\theta0,i}
\sim
\mathcal N
\left(
0,\frac{k_{\mathrm B}T_{i,s_i}}{m_{s_i}}
\right)
$$

- $k_{\mathrm B}$：ボルツマン定数
- $T_{i,s_i}$：粒子 $i$ のイオン種に設定した初期温度
- $m_{s_i}$：粒子 $i$ のイオン質量

接線方向入射傾斜は

$$
p_{\theta,i}=\frac{v_{\theta0,i}}{u_{n,i}}
$$

となる。

接線方向熱速度の平均は0であるが、$u_{n,i}$ はパルス位相によって変化する。このため、一定の熱角度を後から一律に加えるのではなく、各衝突粒子の $u_{n,i}$ を使って接線方向の角度幅を求める。

---

## 3. 粒子の重み付け

### 3.1 粒子軌道そのものを評価する場合

マクロ粒子重みを使用する。

$$
w_i=W_i
$$

すべてのマクロ粒子が同じ実粒子数を表す場合は、$W_i=1$ としてよい。

### 3.2 エッチングへの寄与を評価する場合

イオン種、エネルギー、入射角に応じたエッチング収率を掛ける。

$$
w_i^\ast
=W_iY_{s_i}(E_i,\alpha_i)
$$

- $Y_{s_i}$：イオン種 $s_i$ のエッチング収率
- $E_i$：ウェハー衝突時のイオンエネルギー
- $\alpha_i$：ウェハー法線から測った入射角

接線方向熱速度をサンプリングしている場合、入射角は

$$
\alpha_i
=
\tan^{-1}
\left(
\sqrt{p_{R,i}^2+p_{\theta,i}^2}
\right)
$$

で求める。

収率モデルが未確定の場合は、最初に $w_i=W_i$ で軌道分布を評価する。その結果は「イオン入射分布の評価値」と呼び、加工後形状の予測値とは区別する。

### 3.3 加工有効フラックス

半径領域の面積を $A_R$、集計時間を $\Delta t$ とすると、加工有効フラックスは

$$
\Gamma_{\mathrm{eff}}(R)
=
\frac{\sum_i w_i^\ast}{A_R\Delta t}
$$

である。

環状領域の内外半径を $R_-,R_+$ とすると、

$$
A_R=\pi(R_+^2-R_-^2)
$$

となる。

$Q_{\mathrm{spot}}$ が同程度でも、$\Gamma_{\mathrm{eff}}$ が大きく異なれば、加工深さや形状への寄与は同じではない。このため、形状指標と一緒に確認する。

---

## 4. 平均入射傾斜

イオン束全体の平均半径方向傾斜を

$$
\mu_R
=
\frac{\sum_iw_ip_{R,i}}
{\sum_iw_i}
$$

と定義する。

- $\mu_R>0$：平均として外周方向へ傾いている
- $\mu_R<0$：平均としてウェハー中心方向へ傾いている
- $\mu_R\approx0$：平均方向の系統的な傾きが小さい

穴深さを $H$ とすると、直線軌道近似における底部中心の移動量は

$$
\Delta R_{\mathrm{bottom}}
\approx H\mu_R
$$

となる。

$\mu_R$ は、主に穴軸の傾きや底部中心ずれに関係する。輪郭を再中心化した後の楕円変形とは別の現象であるため、独立した指標として保存する。

---

## 5. パルスによる平均入射方向の変動

### 5.1 衝突位相

衝突時刻からパルス位相を求める。

$$
\phi_i
=
2\pi
\frac{\operatorname{mod}(t_i-t_0,T_{\mathrm p})}
{T_{\mathrm p}}
$$

- $t_0$：パルス位相の基準時刻
- $T_{\mathrm p}$：パルス周期
- $\phi_i$：粒子がウェハーへ衝突したときの位相

放出位相ではなく衝突位相を使用する。イオンの飛行時間によって、電極電圧の変化とウェハー到達時刻の間に遅れが生じるためである。

### 5.2 位相ごとの平均入射傾斜

位相0から $2\pi$ を複数のビン $b$ に分ける。位相ビン $b$ の合計重みを

$$
W_b=\sum_{i\in b}w_i
$$

とし、位相ごとの平均半径方向傾斜を

$$
\mu_{R,b}
=
\frac{\sum_{i\in b}w_ip_{R,i}}
{W_b}
$$

とする。

全位相の平均は

$$
\mu_R
=
\frac{\sum_bW_b\mu_{R,b}}
{\sum_bW_b}
$$

である。

### 5.3 パルススイング

位相ごとの平均方向が、全位相平均からどれだけ変動するかを

$$
\sigma_{\mathrm{swing},R}
=
\sqrt{
\frac{
\sum_bW_b(\mu_{R,b}-\mu_R)^2
}{
\sum_bW_b
}
}
$$

で表す。

- $\sigma_{\mathrm{swing},R}=0$：パルス位相による平均方向の変化がない
- 値が大きい：パルスに同期して平均入射方向が大きく振れている

平方根まで取った量を標準偏差 $\sigma$ とする。平方根を取らない量は分散 $\sigma^2$ であり、両者を混同しない。

---

## 6. 同一位相内の半径方向分布幅

同じパルス位相でも、個々の粒子には初期熱速度、衝突、空間的な電場差などによるばらつきがある。その分布幅を

$$
\sigma_{\mathrm{width},R}
=
\sqrt{
\frac{
\sum_b\sum_{i\in b}
w_i(p_{R,i}-\mu_{R,b})^2
}{
\sum_bW_b
}
}
$$

と定義する。

ここで、

- $\sigma_{\mathrm{swing},R}$：位相ごとの平均方向の変動
- $\sigma_{\mathrm{width},R}$：同一位相内の粒子間ばらつき

である。

半径方向の全分散は、全分散の法則により

$$
\sigma_R^2
=
\sigma_{\mathrm{swing},R}^2
+
\sigma_{\mathrm{width},R}^2
$$

となる。

パルスが半径方向分布全体に占める寄与率を確認したい場合は、

$$
f_{\mathrm{swing}}
=
\frac{\sigma_{\mathrm{swing},R}^2}
{\sigma_R^2}
$$

を使用する。

- $f_{\mathrm{swing}}\approx0$：半径方向分布幅の多くが位相内ばらつき
- $f_{\mathrm{swing}}\approx1$：半径方向分布幅の多くがパルスに同期した変動

---

## 7. 接線方向の熱的な分布幅

接線方向速度を初期熱速度のみとする場合、ランダム速度を各粒子へ実際に割り当てなくても、接線方向分散を解析的に計算できる。

$$
\sigma_{\theta,\mathrm{th}}^2
=
\frac{
\sum_iw_i
\dfrac{k_{\mathrm B}T_{i,s_i}}
{m_{s_i}u_{n,i}^2}
}{
\sum_iw_i
}
$$

$$
\sigma_{\theta,\mathrm{th}}
=
\sqrt{\sigma_{\theta,\mathrm{th}}^2}
$$

この式は、各イオンの接線方向熱速度分散

$$
\left\langle v_{\theta0,i}^2\right\rangle
=
\frac{k_{\mathrm B}T_{i,s_i}}{m_{s_i}}
$$

を、その粒子の法線方向速度 $u_{n,i}$ で角度分布へ換算したものである。パルスによって $u_{n,i}$ が変化する影響も含まれる。

エッチング収率の角度依存や穴内の3D軌道を計算する場合は、各粒子に

$$
v_{\theta0,i}
\sim
\mathcal N
\left(
0,\frac{k_{\mathrm B}T_{i,s_i}}{m_{s_i}}
\right)
$$

を割り当て、$p_{\theta,i}=v_{\theta0,i}/u_{n,i}$ を生成する。この場合は、乱数シードまたはサンプリング数を変えて結果が安定することを確認する。

---

## 8. ウェハー面でのイオン入射異方性

半径方向と接線方向の分布幅を比較するため、

$$
Q_{\mathrm{ion}}
=
\sqrt{
\frac{
\min(\sigma_R^2,\sigma_{\theta,\mathrm{th}}^2)
}{
\max(\sigma_R^2,\sigma_{\theta,\mathrm{th}}^2)
}
}
$$

と定義する。

- $Q_{\mathrm{ion}}=1$：半径方向と接線方向の分布幅が等しい
- $Q_{\mathrm{ion}}<1$：一方向の分布幅が大きい
- 値の範囲：$0<Q_{\mathrm{ion}}\le1$

どちらの方向へ広がっているかは、符号付き異方性

$$
A_{\mathrm{ion}}
=
\frac{
\sigma_R^2-\sigma_{\theta,\mathrm{th}}^2
}{
\sigma_R^2+\sigma_{\theta,\mathrm{th}}^2
}
$$

で確認する。

- $A_{\mathrm{ion}}>0$：半径方向の分布が広い
- $A_{\mathrm{ion}}<0$：接線方向の分布が広い
- $A_{\mathrm{ion}}\approx0$：両方向の分布幅が同程度

この式は、接線方向熱速度が半径方向運動と独立であり、両者の共分散が0とみなせることを前提としている。

$Q_{\mathrm{ion}}$ はウェハー面での入射角度分布を評価する中間指標であり、穴径や穴深さは含まない。

---

## 9. 穴径・深さを考慮した最終計算指標

### 9.1 底部到達スポットの近似

入口半径 $a$、評価深さ $H$ の円形穴を考える。入口におけるイオン入射位置が円内で一様で、イオンが直線的に移動し、側壁による遮蔽や反射がないと仮定する。

円内一様分布が持つ1方向当たりの位置分散は

$$
\sigma_{\mathrm{aperture}}^2
=
\frac{a^2}{4}
$$

である。

深さ $H$ での半径方向と接線方向の位置分散は、それぞれ

$$
S_R^2
=
\frac{a^2}{4}
+
H^2\sigma_R^2
$$

$$
S_\theta^2
=
\frac{a^2}{4}
+
H^2\sigma_{\theta,\mathrm{th}}^2
$$

となる。

底部到達スポットの等方性を

$$
Q_{\mathrm{spot}}
=
\sqrt{
\frac{
\min(S_R^2,S_\theta^2)
}{
\max(S_R^2,S_\theta^2)
}
}
$$

で定義する。

- $Q_{\mathrm{spot}}=1$：底部到達スポットが等方的
- $Q_{\mathrm{spot}}$ が小さい：一方向に広がったスポット
- 穴が深い、または入射角度分布の異方性が大きいほど、通常は $Q_{\mathrm{spot}}$ が小さくなる

これを、本検討における計算側の最終指標とする。

### 9.2 この指標が表す値

$Q_{\mathrm{spot}}$ は、穴深さ位置におけるイオン到達位置分布の短軸方向RMS幅と長軸方向RMS幅の比である。

平均入射傾斜 $\mu_R$ は分散計算時に除かれている。このため、$Q_{\mathrm{spot}}$ は底部中心を再中心化した後の広がりを表す。底部中心ずれは $H\mu_R$ で別途評価する。

### 9.3 適用限界

次の条件では、$Q_{\mathrm{spot}}$ だけで実験穴形状を説明できない可能性が高い。

- 高アスペクト比で側壁遮蔽が強い
- 側壁や底面の帯電によって軌道が曲がる
- 角度によってイオン反射率やエッチング収率が大きく変化する
- ラジカル供給やパッシベーションが形状を支配する
- マスク形状が既に楕円または偏心している

この場合は、穴内到達ドーズ分布へ評価を拡張する。

---

## 10. 高アスペクト比穴に対する拡張

接線方向熱速度をサンプリングし、各粒子の横方向傾斜ベクトルを

$$
\boldsymbol p_i
=
\begin{pmatrix}
p_{R,i}\\
p_{\theta,i}
\end{pmatrix}
$$

とする。

深さ $H$ まで直線的に進んだときの横方向移動量は

$$
\boldsymbol d_i
=
H\boldsymbol p_i
$$

である。

底部位置を $\boldsymbol x$ としたとき、入口位置は $\boldsymbol x-\boldsymbol d_i$ となる。半径 $a$ の円筒穴で、側壁へ到達したイオンを吸収すると仮定した底部ドーズ分布は、相対値として

$$
D_H(\boldsymbol x)
=
\sum_iw_i^\ast
\,\mathbf 1(|\boldsymbol x|\le a)
\,\mathbf 1(|\boldsymbol x-\boldsymbol d_i|\le a)
$$

で計算できる。

- $D_H(\boldsymbol x)$：底部位置 $\boldsymbol x$ の加工有効イオンドーズ
- $\mathbf 1(\cdot)$：条件成立時に1、非成立時に0となる関数
- $w_i^\ast$：エッチング収率まで考慮した粒子重み

このドーズ分布の等値線から、実験穴と同じ方法で楕円成分を求める。$Q_{\mathrm{spot}}$ より計算量は増えるが、穴深さによる角度選別と側壁遮蔽をより直接的に扱える。

---

## 11. 実験穴輪郭の評価方法

### 11.1 輪郭の再中心化

穴輪郭を評価する前に、輪郭点または面積重心から穴中心を求め、座標原点を穴中心へ移動する。

中心ずれを残したまま楕円成分を計算すると、穴軸傾斜に相当する1次成分が真円度評価へ混入する。

再中心化後の輪郭を、穴内極座標 $(\rho,\varphi)$ で表す。

### 11.2 楕円成分

輪郭半径を

$$
\rho(\varphi)
=
\rho_0
+
c_2\cos(2\varphi)
+
s_2\sin(2\varphi)
+
\cdots
$$

と展開する。

- $\rho_0$：平均穴半径
- $c_2,s_2$：楕円状変形を表す2次成分

等角度間隔の $N$ 点で輪郭を取得した場合は、

$$
\rho_0
=
\frac{1}{N}\sum_{k=1}^{N}\rho_k
$$

$$
c_2
=
\frac{2}{N}
\sum_{k=1}^{N}
(\rho_k-\rho_0)\cos(2\varphi_k)
$$

$$
s_2
=
\frac{2}{N}
\sum_{k=1}^{N}
(\rho_k-\rho_0)\sin(2\varphi_k)
$$

で求める。

楕円変形量は

$$
\varepsilon_2
=
\frac{\sqrt{c_2^2+s_2^2}}{\rho_0}
$$

とする。

- $\varepsilon_2=0$：楕円成分がない
- $\varepsilon_2$ が大きい：楕円状の変形が大きい

主軸方向は

$$
\psi_2
=
\frac{1}{2}
\operatorname{atan2}(s_2,c_2)
$$

で求める。

小変形の楕円を軸比で表す場合は、近似的に

$$
Q_{\mathrm{exp}}
\approx
\frac{1-\varepsilon_2}
{1+\varepsilon_2}
$$

と表せる。ただし、主評価値は方向情報を保持できる $\varepsilon_2$ と $\psi_2$ とする。

---

## 12. 計算と実験の比較方法

### 12.1 半径方向傾向

各ウェハー半径について、次を比較する。

- 計算：$1-Q_{\mathrm{spot}}(R)$
- 実験：$\varepsilon_2(R,\Theta)$ の同一半径内平均とばらつき

中心から外周へ向かう増減傾向、装置条件変更に対する増減方向、変化が大きくなる半径領域を確認する。

### 12.2 楕円方向

2D軸対称モデルに由来する異方性であれば、実験穴の主軸方向は局所的な半径方向または接線方向になると考えられる。

ウェハー上の穴位置角を $\Theta$ としたとき、

$$
\psi_2-\Theta
$$

が0付近または $\pi/2$ 付近へ集まるかを確認する。

同じ半径で楕円変形量が方位角ごとに大きく異なる場合や、楕円軸がウェハー全体で同じ装置固定方向を向く場合は、2D軸対称モデル以外の要因を検討する。

候補としては、給電・リターン経路、ガス導入、排気、フォーカスリングの非対称、ESC構造、磁場、温度分布、マスク方向などがある。

### 12.3 比較の進め方

最初の段階では、絶対値の一致よりも以下を確認する。

1. 半径方向の変化傾向が一致するか。
2. パルス振幅やDuty比を変えたときの増減方向が一致するか。
3. 主軸方向が半径方向または接線方向になるか。
4. $\mu_R$ と穴中心ずれ、$1-Q_{\mathrm{spot}}$ と $\varepsilon_2$ がそれぞれ対応するか。

傾向が確認できた後に、同一材料、同一マスク、同一レシピ群の実験から換算係数を校正する。

---

## 13. 具体的な計算手順

### Step 1：周期定常状態を選ぶ

パルス電圧、シース電場、ウェハー到達イオン数、平均エネルギーが周期ごとに大きく変化しなくなった期間を選ぶ。初期過渡期間の粒子は除外する。

### Step 2：半径方向環状領域を設定する

ウェハーを半径方向の環状領域に分割する。

環状領域の幅は実際の穴径に合わせる必要はない。統計的に十分な衝突粒子数を確保できる幅で計算し、幅を変更して結果が安定することを確認する。

### Step 3：衝突イベントを抽出する

各粒子について、$R_i,t_i,v_{R,i},v_{z,i},W_i,s_i$ を保存する。可能であれば $E_i$、衝突履歴、粒子IDも保存する。

同じ粒子を複数回数えていないこと、ウェハーへの到達イベントだけを抽出していることを確認する。

### Step 4：入射傾斜を計算する

各衝突粒子について、

$$
u_{n,i}=|v_{z,i}|,
\qquad
p_{R,i}=\frac{v_{R,i}}{u_{n,i}}
$$

を計算する。

接線方向は、初期イオン温度と質量から $\sigma_{\theta,\mathrm{th}}$ を解析的に求める。

### Step 5：衝突位相で分類する

衝突時刻から $\phi_i$ を求め、位相ビンごとに粒子を分類する。

初期設定として1周期を36分割程度とし、24分割、72分割などでも指標が大きく変わらないことを確認する。重要なのは分割数そのものではなく、波形変化を捉えられ、各ビンの粒子数も確保できることである。

### Step 6：平均と分散を計算する

各半径領域について、以下を順に計算する。

1. $\mu_{R,b}$：位相ごとの平均傾斜
2. $\mu_R$：全位相の平均傾斜
3. $\sigma_{\mathrm{swing},R}$：位相間の変動
4. $\sigma_{\mathrm{width},R}$：位相内のばらつき
5. $\sigma_R$：半径方向の全分布幅
6. $\sigma_{\theta,\mathrm{th}}$：接線方向熱分布幅
7. $Q_{\mathrm{ion}},A_{\mathrm{ion}}$：ウェハー面での異方性
8. $Q_{\mathrm{spot}}$：穴径・深さを考慮した最終指標

### Step 7：粒子数重みと加工有効重みを比較する

まず $w_i=W_i$ で計算する。

収率が定義できる場合は、$w_i^\ast=W_iY_{s_i}(E_i,\alpha_i)$ で再計算する。

両者の傾向が異なる場合は、角度の大きな低エネルギーイオンなど、加工への寄与が小さい粒子が初期結果を支配していないか確認する。

### Step 8：実験輪郭と比較する

実験穴ごとに $\varepsilon_2,\psi_2$ を求め、半径 $R$ と装置方位角 $\Theta$ を付けて整理する。

計算側の $1-Q_{\mathrm{spot}}$ と半径方向傾向を比較し、$\psi_2-\Theta$ から主軸方向を確認する。

---

## 14. 計算結果の確認事項

### 14.1 統計的な収束

- 半径領域の幅を変えても指標が大きく変わらないか。
- 位相ビン数を変えても $\sigma_{\mathrm{swing},R}$ が安定しているか。
- 使用周期数と粒子数を増やしても結果が安定しているか。
- 位相ビンごとの粒子数が極端に少なくないか。

粒子重みが不均一な場合は、有効サンプル数

$$
N_{\mathrm{eff}}
=
\frac{(\sum_iw_i)^2}
{\sum_iw_i^2}
$$

も確認する。

見かけ上の粒子数が多くても、少数の大きな重みが結果を支配している場合は、$N_{\mathrm{eff}}$ が小さくなる。

### 14.2 物理的な整合性

- パルスを停止した条件で $\sigma_{\mathrm{swing},R}$ が十分小さくなるか。
- $T_i$ を下げたときに $\sigma_{\theta,\mathrm{th}}$ が小さくなるか。
- 法線方向加速が強くなったときに、同じ横速度でも入射傾斜が小さくなるか。
- 軸中心付近で半径方向の符号や座標特異性が問題になっていないか。
- $\sigma_R^2=\sigma_{\mathrm{swing},R}^2+\sigma_{\mathrm{width},R}^2$ が数値的に成立するか。

### 14.3 前提に対する感度

- 初期イオン温度 $T_i$ の設定範囲を変えたときに結論が変わらないか。
- 穴半径 $a$ と深さ $H$ を実験条件に合わせたか。
- 収率モデルの有無で $Q_{\mathrm{spot}}$ の半径方向傾向が変わらないか。
- 接線方向速度を初期熱速度のまま保持する仮定が、半径移動の大きな軌道でも妥当か。

接線方向速度を初期熱速度のままとする扱いは、接線方向衝突散乱と角運動量変化を省略した近似である。半径移動量が大きい条件では、この近似に対する感度確認が必要になる。

---

## 15. 評価図参考


### 位相別の計算結果

- 横軸：パルス位相
- 縦軸：$\mu_{R,b}$
- 別軸または別図：位相別フラックス、平均エネルギー

この図で、どの位相に入射方向が変化し、加工に有効なイオンがどの位相に到達するかを示す。

### 半径方向の指標

- $\mu_R(R)$
- $\sigma_{\mathrm{swing},R}(R)$
- $\sigma_{\mathrm{width},R}(R)$
- $Q_{\mathrm{ion}}(R)$
- $Q_{\mathrm{spot}}(R)$


### 実験との比較

- 横軸：ウェハー半径 $R$
- 計算：$1-Q_{\mathrm{spot}}(R)$
- 実験：$\varepsilon_2(R,\Theta)$ の平均とばらつき
- 主軸方向：$\psi_2-\Theta$ の分布

### 結論欄

結論では、次の3点を分けて書く。

1. 計算から確認できた事実
2. 実験との比較から支持された解釈
3. 現モデルでは判断できない事項

---

